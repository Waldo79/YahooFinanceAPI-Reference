# Field Dictionary

Each field should include field name, JSON path, endpoint, security types, marketState observations, data type, units, description, related fields, formula if derived, nullability, example values, provenance, and confidence rating.
