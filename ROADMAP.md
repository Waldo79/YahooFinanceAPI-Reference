# Roadmap

## v0.1.0 - Foundation
Establish repository structure and documentation approach.

## v0.2.0 - Quote Endpoint
Complete Quote endpoint documentation, field dictionary entries, sample captures, and CSV mapping.

## v0.3.0 - Chart Endpoint
Document chart `meta`, `timestamp`, `indicators`, events, OHLCV CSV mapping, and timestamp conversion notes.

## v0.4.0 - QuoteSummary Modules
Document `summaryDetail`, `defaultKeyStatistics`, `financialData`, `fundProfile`, `summaryProfile`, `calendarEvents`, and `quoteType`.

## v0.5.0 - Search and Screener
Document symbol lookup fields, predefined screeners, and examples.

## v1.0.0 - Initial Complete Reference
Field occurrence matrix, security-type matrix, marketState matrix, validation suite, and generated Excel workbook.
